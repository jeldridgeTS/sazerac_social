<h1>Articles</h1>

<div class="container moo">
  <div class="card-deck">
    <div class="row">
      <%= render @articles %>
    </div>
  </div>
</div>

<br>

<% if policy(Article).new? %>
  <%= link_to 'New Article', new_article_path %>
<% end %>
