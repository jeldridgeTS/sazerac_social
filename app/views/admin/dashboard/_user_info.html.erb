<% if !user.nil? %>
  <p>
    <span class="email"><%= user.email %></span>
    <span class="display-name"><%= user.display_name %></span>
    <span class="assigned-roles"><%= user.user_roles %></span>
  </p>
<% end %>
